import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css';

// a single call to the react engine, asking it to render an App component
ReactDOM.render(
  <App />,
  document.getElementById('root')
);